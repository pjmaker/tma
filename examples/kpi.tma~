# -*-tcl-*-
#
# kpi.tma
#

report:new outage_summary

st 2013-4-1


foreach region {A D K T} {
  foreach tag [pi:tags R_${region}_PG_*_F*_F] {
    get $tag
    check:nonzero $tag
  }
  title "Feeders within Region ${region}"
  report:plot 2i
  var- *
}
report:show
